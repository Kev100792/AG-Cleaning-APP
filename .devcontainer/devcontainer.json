{
  "name": "Flutter Web Dev",
  "image": "ghcr.io/cirruslabs/flutter:stable",
  "features": {},
  "customizations": {
    "vscode": {
      "extensions": [
        "Dart-Code.dart-code",
        "Dart-Code.flutter",
        "esbenp.prettier-vscode"
      ]
    }
  },
  "forwardPorts": [8080],
  "postCreateCommand": "flutter --version && flutter config --enable-web && git config --global --add safe.directory /workspaces/${containerWorkspaceFolderBasename}"
}
